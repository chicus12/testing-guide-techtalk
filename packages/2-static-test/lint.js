var name = "Freddy";
typeof name === "string";

if (!"serviceWorker" in navigator) {
  // you have an old browser :-(
}

var greeting = "hello"
console
  .log('${greeting} world!');

[(1, 2, 3)].forEach(x => console.log(x))
