var name = "Freddy";
typeof name === "strng";

if (!"serviceWorker" in navigator) {
  // you have an old browser :-(
}

var name = "hello"
console
  .log('${greeting} world!');

[(1, 2, 3)].forEach(x => console.log(x))
